<%
/* ### Generated by AIDesigner 3.7.1.22 ### */
%>

<%@ page language="java" import="java.sql.*,java.io.*,java.text.*" %>
<%@ page contentType="text/plain; charset=euc-kr" %>

<%
Object[] userObj = null;
ReportHandler handler = new ReportHandler(request,response,userObj);
handler.service(out);
%>

<%!
class ReportHandler{
	//공통 변수 선언
	private Object[] userObj = null;
	private int nStartPage;		//각 쉬트별 시작 페이지 저장
	private int nMaxPage;		//한 쉬트가 실행된 후 생성된 최대 페이지 번호 저장
	private int nPageNum;		//현재 페이지 번호
	private int nYStartPos;		//쉬트에서 Y 시작 좌표
	private int nYEndPos;		//쉬트에서 Y 끝 좌표
	private int nYCurPos;		//현재 Y 좌표
	private int nYSavePos;		//연결된 멀티 프레임 쉬트에서 시작 Y 좌표 저장
	private int nYLastPos;		//연결된 멀티 프레임 쉬트에서 각 프레임의 마지막 Y 좌표 저장
	private int nXStartPos;		//쉬트에서 X 시작 좌표
	private int nXEndPos;		//쉬트에서 X 끝 좌표
	private int nXCurPos;		//현재 X 좌표
	private int nXSavePos;		//연결된 멀티 프레임 쉬트에서 시작 X 좌표 저장
	private int  nXLastPos;		//연결된 멀티 프레임 쉬트에서 각 프레임의 마지막 X 좌표 저장
	private boolean bIsBeforePageSkip;		//조건 밴드에서 페이지 넘기기 여부 판단
	private int nLoopRef;		//절대 양식에서 루프 참조 변수
	private String isEmpty="";		//빈 문자열 비교에 사용

	private HttpServletRequest request;
	private HttpServletResponse response;
	private JspWriter out;

	//-----데이터 변수 선언-----
	String sheet10_A1;
	long sheet10_A2;
	String sheet10_B2;
	double sheet10_A3;
	String sheet10_B3;
	double sheet10_C3;
	double sheet10_D3;
	String AI_CHART_OBJ2;

	//-----Connection, Statement, ResultSet 객체 선언-----
	private Connection conn_DUNET;		//DSN DUNET Connection 객체
	private java.sql.Statement stat_chart;		//ResultSet chart 객체를 생성하기 위한 Statement 객체
	private ResultSet chart;		//ResultSet chart 객체
	private java.sql.Statement stat_master;		//ResultSet master 객체를 생성하기 위한 Statement 객체
	private ResultSet master;		//ResultSet master 객체
	private java.sql.Statement stat_detail;		//ResultSet detail 객체를 생성하기 위한 Statement 객체
	private ResultSet detail;		//ResultSet detail 객체

	//-----SQL 변수 선언-----
	private String sql_chart;		//ResultSet chart 객체를 생성하기 위한 쿼리문 저장
	private String sql_master;		//ResultSet master 객체를 생성하기 위한 쿼리문 저장
	private String sql_detail;		//ResultSet detail 객체를 생성하기 위한 쿼리문 저장

	//-----쿼리 필드 변수 선언-----
	//ResultSet chart 객체에서 필드 값을 받는 쿼리 필드 변수
	private double chart_QUESTION_NO;
	private String chart_QUESTION;
	private double chart_ANSWER_NO;
	private String chart_ANSWER;
	private double chart_ANSWER_KIND;
	private double chart_TOTAL;
	private double chart_ANS_NO_TOTAL;

	//ResultSet master 객체에서 필드 값을 받는 쿼리 필드 변수
	private double master_QUESTION_NO;
	private double master_SET_NO;
	private String master_QUESTION;

	//ResultSet detail 객체에서 필드 값을 받는 쿼리 필드 변수
	private double detail_QUESTION_NO;
	private String detail_QUESTION;
	private String detail_SUBJNM;
	private double detail_ANSWER_NO;
	private String detail_ANSWER;
	private double detail_ANSWER_KIND;
	private double detail_TOTAL;
	private double detail_ANS_NO_TOTAL;

	//파라미터 변수 선언
	String p_title_no_Param;
	String p_set_no_Param;

	//-----사용자 정의 변수 및 함수-----
	int nYTemp=0;
	int chart_no = 1;


	//ReportHandler 생성자 함수
	ReportHandler(HttpServletRequest request,HttpServletResponse response,Object[] userObj){
		this.request=request;
		this.response=response;
		this.userObj=userObj;
	}

	//ReportHandler 서비스 함수
	void service(JspWriter out) throws Exception{
		try{    //try 구문
			this.out=out;
			nMaxPage=0;    //nMaxPage 초기화

			//스크립트 헤더 함수 호출
			getScriptHeader("A4", "2100x2970", "9", "VERTICAL");

			//파라미터 설정 함수 호출
			setParam();

			//고정 데이터 초기화 함수 호출
			initConstVarient();

			//Connection 및 Statement 객체 생성 함수 호출
			createConnAndStateObject();

			//쿼리문 초기화 함수 호출
			initSQLConst();

			//쉬트 함수 호출
			//SHEET=Sheet1   FRAME=0 함수 호출
			executeSheet10();

			//---------------스크립트 종료---------------
			out.print("--SCRIPT_END--\r");
		}catch(Exception e){    //catch 구문
			out.print(e);
			out.print("AI!REPORT JSP ERROR!!!!\r");
		}finally{    //finally 구문
			try{    //finally try 구문
				//DataSet Close
				if(stat_detail!=null) stat_detail.close();
				if(master!=null) master.close();
				if(stat_master!=null) stat_master.close();
				if(chart!=null) chart.close();
				if(stat_chart!=null) stat_chart.close();
				if(conn_DUNET!=null) conn_DUNET.close();
			}catch(Exception e){    //finally catch 구문
			}    //end finally try
		}    //end try
	}

	//-----쉬트 함수-----
	//-----SHEET=Sheet1   FRAME=Frame0 Start
	//-----폼 형식 : 상대 양식
	private void executeSheet10( ) throws Exception{
		//쉬트 초기화 함수 호출
		initSheetVariant(41, 71, 546, 801, true);

		//시작 페이지 번호 출력
		out.print("-- " + nPageNum + " PAGE --\r");

		//밴드 함수 호출
		getScriptFromheader(true, true);    //머리글 밴드 호출
		getScriptFrommaster(true);    //반복 밴드 호출

		//페이지 및 좌표 설정
		nMaxPage=(int)MAX((long)nMaxPage, (long)nPageNum);    //생성된 최대 페이지 번호 구하기
	}

	//-----밴드 함수-----
	//----header HeaderBand Start----
	//----소속 쉬트 : SHEET=Sheet1    FRAME=Frame0
	//----밴드 이름 : header
	//----밴드 종류 : 머리글 밴드
	//----bIsFrist : 쉬트 함수에서 최초로 호출될 때 true, 페이지 스킵 함수에서 호출될 때 false
	//----bIsPrint : 감추기 속성 설정 여부. treu면 출력, false면 AI 스크립트를 생성하지 않음.
	private void getScriptFromheader(boolean bIsFirst, boolean bIsPrint) throws Exception{
		if(bIsFirst){   //밴드 최초 호출 시에만 실행되는 코드
		}   //end if(bIsFirst)

		//AI 스크립트 출력
		if(bIsPrint){   //감추기 속성이 지정되지 않으면 bIsPrint는 true
			//스크립트 출력 함수 호출
			writeScriptheader();
		}   //end if(bIsPrint)
	}

	//----chart VoidBand Start----
	//----소속 쉬트 : SHEET=Sheet1    FRAME=Frame0
	//----밴드 이름 : chart
	//----밴드 종류 : Void 밴드
	//----bIsFrist : 부모 밴드 함수나 쉬트 함수에서 최초로 호출될 때 true, 그 이후 호출될 때 false
	//----bIsPrint : 감추기 속성 설정 여부. treu면 출력, false면 AI 스크립트를 생성하지 않음.
	//----nRepeatCnt : 부모 반복 밴드 수행 횟수와 연동 옵션 설정시 void 밴드가 수행할 반복 횟수 전달
	private void getScriptFromchart(boolean bIsFirst, boolean bIsPrint) throws Exception{
		nYCurPos = nYTemp;

		//지역 변수 선언 및 초기화
		int nBandHeight=100;    //밴드 출력 높이

		if(bIsFirst){   //밴드 최초 호출 시에만 실행되는 코드
			//데이터 변수 초기화
			AI_CHART_OBJ2="";

			//데이터 변수 할당
			getChartDataAI_CHART_OBJ2( );
		}    //end if(bIsFirst)

		//AI 스크립트 출력
		if(bIsPrint){   //감추기 속성이 지정되지 않으면 bIsPrint는 true
			//스크립트 출력 영역 검사
			if(isPageSkip(nYCurPos+nBandHeight)){
				pageSkip(10);
			}

			//스크립트 출력 함수 호출
			writeScriptchart();

			//다음 출력 좌표 설정
			nYCurPos=nYCurPos+nBandHeight;
		}   //end if(bIsPrint)
	}

	//-----detail RepeatBand Start-----
	//-----소속 쉬트 : SHEET=Sheet1   FRAME=Frame0
	//-----밴드 이름 : detail
	//-----밴드 종류 : 반복 밴드
	//-----bIsPrint : 감추기 속성 설정 여부 true면 출력, false면 스크립트를 생성하지 않음
	private void getScriptFromdetail(boolean bIsPrint) throws Exception{
		//지역 변수 선언 및 초기화
		int nBandHeight=20;    //밴드 출력 높이
		boolean bIsFirst=true;

		//데이터 변수 초기화
		sheet10_A3=0;
		sheet10_B3="";
		sheet10_C3=0;
		sheet10_D3=0;

		//DataSet 객체 생성
		//ResultSet detail 객체의 생성
		initSQLdetailVarient();
		detail = stat_detail.executeQuery(sql_detail);

		while(detail.next()){   //자동으로 생성되는 반복 밴드의 루프문
			//데이터 fetch
			setFieldVariableFromdetail();	//RecordSet detail에서 필드 값 설정

			if(bIsFirst){   //밴드 호출 후 루프 내에서 한번 만 실행되는 코드
			}   //end if(bIsFirst)

			//데이터 변수 할당
			sheet10_A3=detail_ANSWER_NO;
			sheet10_B3=detail_ANSWER;
			sheet10_C3=detail_TOTAL;
			sheet10_D3=detail_ANS_NO_TOTAL;

			//AI 스크립트 출력
			if(bIsPrint){   //감추기 속성이 지정되지 않으면 bIsPrint는 true
				if(isPageSkip(nYCurPos+nBandHeight)){   //스크립트 출력 영역 검사
					//페이지 스킵 함수 호출
					pageSkip(10);
				}   //end if(page skip test)

				//스크립트 출력 함수 호출
				writeScriptdetail();

				//다음 출력 좌표 설정
				nYCurPos=nYCurPos+nBandHeight;
			}   //end if(bIsPrint)

			//bIsFirst 및 출력 라인 설정
			bIsFirst=false;
		}   //while end

		//루프 외부 자식 밴드 호출
		getScriptFromchart(true, true);

		//DataSet Close
		if(detail!=null) detail.close();
	}

	//-----master RepeatBand Start-----
	//-----소속 쉬트 : SHEET=Sheet1   FRAME=Frame0
	//-----밴드 이름 : master
	//-----밴드 종류 : 반복 밴드
	//-----bIsPrint : 감추기 속성 설정 여부 true면 출력, false면 스크립트를 생성하지 않음
	private void getScriptFrommaster(boolean bIsPrint) throws Exception{
		//지역 변수 선언 및 초기화
		int nBandHeight=20;    //밴드 출력 높이
		boolean bIsFirst=true;

		//데이터 변수 초기화
		sheet10_A2=0L;
		sheet10_B2="";

		//DataSet 객체 생성
		//ResultSet master 객체의 생성
		if(master==null){
			master = stat_master.executeQuery(sql_master);
		}else{
			master.beforeFirst();
		}

		while(master.next()){   //자동으로 생성되는 반복 밴드의 루프문
			//데이터 fetch
			setFieldVariableFrommaster();	//RecordSet master에서 필드 값 설정

			if(bIsFirst){   //밴드 호출 후 루프 내에서 한번 만 실행되는 코드
			}   //end if(bIsFirst)

			//데이터 변수 할당
			sheet10_A2=COUNT(sheet10_A2);
			sheet10_B2=master_QUESTION;

			//AI 스크립트 출력
			if(bIsPrint){   //감추기 속성이 지정되지 않으면 bIsPrint는 true
				if(isPageSkip(nYCurPos+nBandHeight)){   //스크립트 출력 영역 검사
					//페이지 스킵 함수 호출
					pageSkip(10);
				}   //end if(page skip test)

				//스크립트 출력 함수 호출
				writeScriptmaster();

				//다음 출력 좌표 설정
				nYCurPos=nYCurPos+nBandHeight;
			}   //end if(bIsPrint)

			nYTemp = nYCurPos;

			//루프 내부 자식 밴드 호출
			getScriptFromdetail(true);

			//bIsFirst 및 출력 라인 설정
			bIsFirst=false;
		}   //while end
	}

	//-----스크립트 출력 함수-----
	//----header 스크립트 문 출력 함수----
	private void writeScriptheader( ) throws Exception{
		//스크립트문 생성
		out.print("^PRINT(41,41,504,1,16,0/,굴림체,RGB[0,0,0],BOLD, , ,NONE,30,1)");
		out.println(sheet10_A1+"\r");
	}

	//----chart 스크립트 문 출력 함수----
	private void writeScriptchart( ) throws Exception{
		/*

		//스크립트문 생성
		out.println("^CHART(" + (nXCurPos+314) + "," + (nYCurPos+2) + ",190,98,repot_76!000.cht)\r");
		if(!AI_CHART_OBJ2.equals(isEmpty)){
			out.print(AI_CHART_OBJ2);
			AI_CHART_OBJ2="";
		}
		out.print("--CHART_END--\r");

		*/
		out.println("^CHART(" + (nXCurPos+314) + "," + (nYCurPos+2) + ",190,98,repot_76!000.cht,XXCH000XX"+String.valueOf(chart_no)+".dat)\r");
		if(!AI_CHART_OBJ2.equals(isEmpty)){
			out.print(AI_CHART_OBJ2);
			AI_CHART_OBJ2="";
		}
		out.print("--CHART_END--\r");
		chart_no++;
	}

	//----detail 스크립트 문 출력 함수----
	private void writeScriptdetail( ) throws Exception{
		//스크립트문 생성
		out.print("^PRINT("+(nXCurPos)+","+(nYCurPos)+",30,0,11,1/0/0,굴림체,RGB[0,0,0], , , ,NONE,20,1)");
		out.println(sheet10_A3+"\r");
		out.print("^PRINT("+(nXCurPos+30)+","+(nYCurPos)+",188,0,11,0/,굴림체,RGB[0,0,0], , , ,NONE,20,1)");
		out.println(sheet10_B3+"\r");
		out.print("^PRINT("+(nXCurPos+218)+","+(nYCurPos)+",47,1,11,1/0/0,굴림체,RGB[0,0,0], , , ,NONE,20,1)");
		out.println(sheet10_C3+"\r");
		out.print("^PRINT("+(nXCurPos+265)+","+(nYCurPos)+",47,1,11,1/0/0,굴림체,RGB[0,0,0], , , ,NONE,20,1)");
		out.println(sheet10_D3+"\r");
	}

	//----master 스크립트 문 출력 함수----
	private void writeScriptmaster( ) throws Exception{
		//스크립트문 생성
		out.println("^CELLBG("+(nXCurPos)+","+(nYCurPos)+",30,20,1,RGB[230,230,250],RGB[230,230,250],0)\r");
		out.print("^PRINT("+(nXCurPos)+","+(nYCurPos)+",30,2,11,5/### \". \"*,굴림체,RGB[0,0,0], , , ,NONE,20,1)");
		out.println(sheet10_A2+"\r");
		out.println("^CELLBG("+(nXCurPos+30)+","+(nYCurPos)+",474,20,1,RGB[230,230,250],RGB[230,230,250],0)\r");
		out.print("^PRINT("+(nXCurPos+30)+","+(nYCurPos)+",474,0,11,0/,굴림체,RGB[0,0,0], , , ,NONE,20,1)");
		out.println(sheet10_B2+"\r");
	}

	//파라미터 설정 함수
	private void setParam(){
		p_title_no_Param=toKor(GetB(request.getParameter("p_title_no")));
		p_set_no_Param=toKor(GetB(request.getParameter("p_set_no")));
	}

	//-----Connection 및 Statement 객체 생성 함수-----
	private void createConnAndStateObject() throws Exception{
		//DUNET Connection 객체 생성
		Class.forName("oracle.jdbc.driver.OracleDriver").newInstance();
		conn_DUNET=DriverManager.getConnection("jdbc:oracle:thin:@211.214.192.190:1521:KICPA","inchlms","dlscjs");stat_chart=conn_DUNET.createStatement(ResultSet.TYPE_SCROLL_INSENSITIVE,ResultSet.CONCUR_READ_ONLY);
		stat_master=conn_DUNET.createStatement(ResultSet.TYPE_SCROLL_INSENSITIVE,ResultSet.CONCUR_READ_ONLY);
		stat_detail=conn_DUNET.createStatement(ResultSet.TYPE_SCROLL_INSENSITIVE,ResultSet.CONCUR_READ_ONLY);
	}

	//-----SQL문 변수 초기화 함수-----
	private void initSQLConst(){
		sql_chart = "SELECT ";
		sql_chart = sql_chart + "a.question_no as question_no, ";
		sql_chart = sql_chart + "min(a.question) as question, ";
		sql_chart = sql_chart + "b.answer_no as answer_no, ";
		sql_chart = sql_chart + "min(b.answer) as answer, ";
		sql_chart = sql_chart + "min(b.answer_kind) as  answer_kind, ";
		sql_chart = sql_chart + "count(c.userno) as total, ";
		sql_chart = sql_chart + "nvl(sum(decode(c.ans_no,b.answer_no,1,0)),0) as ans_no_total ";
		sql_chart = sql_chart + "FROM  ";
		sql_chart = sql_chart + "TB_SUBJINQ_QUESTION a, ";
		sql_chart = sql_chart + "TB_SUBJINQ_SAMP b, ";
		sql_chart = sql_chart + "TB_SUBJINQ_ANSWER c ";
		sql_chart = sql_chart + "WHERE  ";
		sql_chart = sql_chart + "a.question_checked_no = 0  ";
		sql_chart = sql_chart + "AND a.samp_checked_no = 0  ";
		sql_chart = sql_chart + "AND a.title_no=b.title_no ";
		sql_chart = sql_chart + "AND a.question_no=b.question_no ";
		sql_chart = sql_chart + "AND c.title_no=a.title_no ";
		sql_chart = sql_chart + "AND c.question_no=a.question_no ";
		sql_chart = sql_chart + "GROUP BY  ";
		sql_chart = sql_chart + "a.title_no, ";
		sql_chart = sql_chart + "a.question_no, ";
		sql_chart = sql_chart + "b.answer_no ";
		sql_chart = sql_chart + "ORDER BY ";
		sql_chart = sql_chart + "a.QUESTION_NO,b.answer_no";
		sql_master = "SELECT ";
		sql_master = sql_master + "a.question_no as question_no, ";
		sql_master = sql_master + "a.set_no as set_no, ";
		sql_master = sql_master + "min(a.question) as question ";
		sql_master = sql_master + "FROM  ";
		sql_master = sql_master + "TB_GRINQ_QUESTION_SET a, ";
		sql_master = sql_master + "TB_GRINQ_SAMP_SET b, ";
		sql_master = sql_master + "TB_GRINQ_ANSWER c ";
		sql_master = sql_master + "WHERE  ";
		sql_master = sql_master + "a.title_no = " + p_title_no_Param + " ";
		sql_master = sql_master + "AND a.set_no = " + p_set_no_Param + " ";
		sql_master = sql_master + "AND a.question_checked_no = 0  ";
		sql_master = sql_master + "AND a.samp_checked_no = 0  ";
		sql_master = sql_master + "AND a.title_no=b.title_no ";
		sql_master = sql_master + "AND a.question_no=b.question_no ";
		sql_master = sql_master + "AND a.set_no=b.set_no ";
		sql_master = sql_master + "AND c.title_no=a.title_no ";
		sql_master = sql_master + "AND c.set_no=a.set_no ";
		sql_master = sql_master + "AND c.question_no=a.question_no ";
		sql_master = sql_master + "GROUP BY  ";
		sql_master = sql_master + "a.title_no, ";
		sql_master = sql_master + "a.set_no, ";
		sql_master = sql_master + "a.question_no ";
		sql_master = sql_master + "ORDER BY ";
		sql_master = sql_master + "a.QUESTION_NO";
	}

	private void initSQLdetailVarient(){
		sql_detail = "SELECT ";
		sql_detail = sql_detail + "a.question_no as question_no, ";
		sql_detail = sql_detail + "min(a.question) as question, ";
		sql_detail = sql_detail + "min(searchsubjnm(a.tsubj)) as subjnm, ";
		sql_detail = sql_detail + "b.answer_no as answer_no, ";
		sql_detail = sql_detail + "min(b.answer) as answer, ";
		sql_detail = sql_detail + "min(b.answer_kind) as  answer_kind, ";
		sql_detail = sql_detail + "count(c.userno) as total, ";
		sql_detail = sql_detail + "nvl(sum(decode(c.ans_no,b.answer_no,1,0)),0) as ans_no_total ";
		sql_detail = sql_detail + "FROM  ";
		sql_detail = sql_detail + "TB_GRINQ_QUESTION_SET a, ";
		sql_detail = sql_detail + "TB_GRINQ_SAMP_SET b, ";
		sql_detail = sql_detail + "TB_GRINQ_ANSWER c ";
		sql_detail = sql_detail + "WHERE a.title_no = " + p_title_no_Param + " ";
		sql_detail = sql_detail + "AND a.set_no = " + p_set_no_Param + " ";
		sql_detail = sql_detail + "AND a.question_checked_no = 0  ";
		sql_detail = sql_detail + "AND a.samp_checked_no = 0  ";
		sql_detail = sql_detail + "AND a.title_no=b.title_no ";
		sql_detail = sql_detail + "AND a.question_no=b.question_no ";
		sql_detail = sql_detail + "AND a.set_no=b.set_no ";
		sql_detail = sql_detail + "AND c.title_no=a.title_no ";
		sql_detail = sql_detail + "AND c.set_no=a.set_no ";
		sql_detail = sql_detail + "AND c.question_no=a.question_no ";
		sql_detail = sql_detail + "AND a.question_no=" + master_QUESTION_NO + " ";
		sql_detail = sql_detail + "GROUP BY  ";
		sql_detail = sql_detail + "a.title_no, ";
		sql_detail = sql_detail + "a.set_no, ";
		sql_detail = sql_detail + "a.question_no, ";
		sql_detail = sql_detail + "b.answer_no ";
		sql_detail = sql_detail + "ORDER BY ";
		sql_detail = sql_detail + "a.QUESTION_NO,b.answer_no";
	}

	//-----ResultSet 객체에서 필드 값 설정 함수-----
	//ResultSet chart 객체에서 필드 값 설정 함수
	private void setFieldVariableFromchart() throws Exception{
		chart_QUESTION_NO=chart.getDouble(1);
		chart_QUESTION=GetB(chart.getString(2));
		chart_ANSWER_NO=chart.getDouble(3);
		chart_ANSWER=GetB(chart.getString(4));
		chart_ANSWER_KIND=chart.getDouble(5);
		chart_TOTAL=chart.getDouble(6);
		chart_ANS_NO_TOTAL=chart.getDouble(7);
	}

	//ResultSet chart 객체의 필드 값 초기화 함수
	private void setFieldInitFromchart(){
		chart_QUESTION_NO=0;
		chart_QUESTION="";
		chart_ANSWER_NO=0;
		chart_ANSWER="";
		chart_ANSWER_KIND=0;
		chart_TOTAL=0;
		chart_ANS_NO_TOTAL=0;
	}

	//ResultSet master 객체에서 필드 값 설정 함수
	private void setFieldVariableFrommaster() throws Exception{
		master_QUESTION_NO=master.getDouble(1);
		master_SET_NO=master.getDouble(2);
		master_QUESTION=GetB(master.getString(3));
	}

	//ResultSet master 객체의 필드 값 초기화 함수
	private void setFieldInitFrommaster(){
		master_QUESTION_NO=0;
		master_SET_NO=0;
		master_QUESTION="";
	}

	//ResultSet detail 객체에서 필드 값 설정 함수
	private void setFieldVariableFromdetail() throws Exception{
		detail_QUESTION_NO=detail.getDouble(1);
		detail_QUESTION=GetB(detail.getString(2));
		detail_SUBJNM=GetB(detail.getString(3));
		detail_ANSWER_NO=detail.getDouble(4);
		detail_ANSWER=GetB(detail.getString(5));
		detail_ANSWER_KIND=detail.getDouble(6);
		detail_TOTAL=detail.getDouble(7);
		detail_ANS_NO_TOTAL=detail.getDouble(8);
	}

	//ResultSet detail 객체의 필드 값 초기화 함수
	private void setFieldInitFromdetail(){
		detail_QUESTION_NO=0;
		detail_QUESTION="";
		detail_SUBJNM="";
		detail_ANSWER_NO=0;
		detail_ANSWER="";
		detail_ANSWER_KIND=0;
		detail_TOTAL=0;
		detail_ANS_NO_TOTAL=0;
	}

	//-----고정 데이터 초기화 함수-----
	private void initConstVarient(){
		sheet10_A1="강사설문분석";
	}

	//-----페이지 스킵 관련 함수-----
	//페이지 스킵시 호출되는 함수로서 페이지 번호를 증가시키고 페이지 지시문 출력 그리고 Y좌표 초기화
	//매개변수에 따라 해당 쉬트의 배경 이미지, 머리글 밴드, 바닥글 밴드 함수 호출
	private void pageSkip(int nIndex) throws Exception{
		nXCurPos=nXStartPos;
		nYCurPos=nYStartPos;
		nPageNum=nPageNum+1;
		out.print("-- " + nPageNum + " PAGE --\r");

		switch(nIndex){
			case 10:		//SHEET=Sheet=1   FRAME=0에서 페이지 스킵시
			getScriptFromheader(false, true);		//머리글 밴드 호출
			break;
		}
	}

	//페이지 스킵이 필요한지 검사하는 함수
	private boolean isPageSkip(int nTestPos){
		if(nTestPos>nYEndPos){
			return true;
		}else{
			return false;
		}
	}

	//----AI_CHART_OBJ2 챠트 데이터 함수----
	private void getChartDataAI_CHART_OBJ2( ) throws Exception{
		/*

		//DataSet 객체 생성
		//ResultSet chart 객체의 생성
		if(chart==null){
			chart = stat_chart.executeQuery(sql_chart);
		}else{
			chart.beforeFirst();
		}
		if(!chart.isBeforeFirst()){		//생성된 ResultSet에 레코드가 없으면
			return;
		}

		*/
		chart = stat_chart.executeQuery(sql_detail);

		if(!chart.isBeforeFirst()){		//생성된 ResultSet에 레코드가 없으면
			return;
		}

		//계열(Series) 필드 설정
		AI_CHART_OBJ2="\t";
		AI_CHART_OBJ2=AI_CHART_OBJ2 + "TOTAL\r\n";

		while(chart.next()){   //데이터 루프문
			/*

			//데이터 fetch
			setFieldVariableFromchart();   //RecordSet chart에서 필드 값 설정

			*/

			chart_ANSWER = GetB(chart.getString(5));
			chart_TOTAL = chart.getDouble(7);

			//X축 레이블 필드 설정
			AI_CHART_OBJ2=AI_CHART_OBJ2 + chart_ANSWER + "\t";

			//데이터 설정
			AI_CHART_OBJ2=AI_CHART_OBJ2 + chart_TOTAL + "\r\n";
		}   //while end
	}

	//-----공통 함수 선언-----
	private void getScriptHeader(String paper, String size, String value, String orient) throws Exception{
		out.print("--SCRIPT_START31--\r");
		out.print("PAPER=" + paper + "\r");
		out.print("PAPER_SIZE=" + size + "\r");
		out.print("PAPER_SETTING_VALUE=" + value + "\r");
		out.print("ORIENTATION=" + orient + "\r");
	}

	private void getSheetPaperInfo(String paper, String size, String value, String orient) throws Exception{
		out.print("--PAPER_INFO--\r");
		out.print("PAPER=" + paper + "\r");
		out.print("PAPER_SIZE=" + size + "\r");
		out.print("PAPER_SETTING_VALUE=" + value + "\r");
		out.print("ORIENTATION=" + orient + "\r");
	}

	private void initSheetVariant(int constStartX, int constStartY, int constEndX, int constEndY, boolean bFirstFrame){
		if(bFirstFrame){
			nStartPage=nMaxPage+1;
			nYLastPos=0;
		}
		nPageNum=nStartPage;
		nYStartPos=constStartY;
		nYEndPos=constEndY;
		nYCurPos=nYStartPos;
		nXStartPos=constStartX;
		nXEndPos=constEndX;
		nXCurPos=nXStartPos;
	}

	private void initChildSheetVariant(int constStartX, int constStartY, int constEndX, int constEndY, boolean bIsMultiFrame, boolean bIsFirstFrame){
		nXStartPos=constStartX;
		nXEndPos=constEndX;
		nXCurPos=nXStartPos;
		nYStartPos=constStartY;
		nYEndPos=constEndY;

		if(bIsMultiFrame){
			if(bIsFirstFrame){
				nStartPage=nPageNum;
				nYSavePos=nYCurPos;
				nYLastPos=0;
			}else{
				nPageNum=nStartPage;
				nYCurPos=nYSavePos;
			}
		}else{
			nStartPage=nPageNum;
		}
	}

	//Empty Row만으로 구성되어 출력 위치를 조정하기 위한 VoidBand 함수
	//nRangePos - VoidBand의 출력 영역
	//nIndex - PageSkip 함수 호출시 전달할 쉬트 인덱스
	private boolean voidBandNoneData(int nRangePos, int nIndex) throws Exception{
		int nTestPos;
		nTestPos=nYCurPos+nRangePos;
		if(isPageSkip(nTestPos)){
			pageSkip(nIndex);
			return true;
		}else{
			nYCurPos=nYCurPos+nRangePos;
			return false;
		}
	}

	private String Get0(String str){
		if(str==null){
			return "0";
		}else{
			if(str.equals("")){
				return "0";
			}else{
				return str;
			}
		}
	}

	private String GetB(String str){
		if(str==null){
			return "";
		}else{
			return str;
		}
	}

	private double SUM(double accVal, double tmpVal){
		return(accVal+tmpVal);
	}

	private double SUM(double accVal, long tmpVal){
		return(accVal+(double)tmpVal);
	}

	private long SUM(long accVal, long tmpVal){
		return(accVal+tmpVal);
	}

	private double SUM(long accVal, double tmpVal){
		return((double)accVal+tmpVal);
	}

	private long COUNT(long accuCNT){
		return ++accuCNT;
	}

	private long MIN(long maxV, long maxTempV){
		if(maxV<=maxTempV){return maxV;}
		else{return maxTempV;}
	}

	private double MIN(long maxV, double maxTempV){
		if((double)maxV<=maxTempV){return (double)maxV;}
		else{return maxTempV;}
	}

	private double MIN(double maxV, double maxTempV){
		if(maxV<=maxTempV){return maxV;}
		else{return maxTempV;}
	}

	private double MIN(double maxV, long maxTempV){
		if(maxV<=maxTempV){return maxV;}
		else{return (double)maxTempV;}
	}
	private long MAX(long maxV, long maxTempV){
		if(maxV>=maxTempV){return maxV;}
		else{return maxTempV;}
	}

	private double MAX(long maxV, double maxTempV){
		if((double)maxV>=maxTempV){return (double)maxV;}
		else{return maxTempV;}
	}

	private double MAX(double maxV, double maxTempV){
		if(maxV>=maxTempV){return maxV;}
		else{return maxTempV;}
	}

	private double MAX(double maxV, long maxTempV){
		if(maxV>=maxTempV){return maxV;}
		else{return (double)maxTempV;}
	}

	private double AVG(double sumVal, long cntVal){
		return(sumVal/cntVal);
	}

	private double AVG(long sumVal, long cntVal){
		return((double)sumVal/(double)cntVal);
	}

	private String aiReplace(String strAll, String strSrc, String strDest) {
		while(strAll.indexOf(strSrc) != -1) {
			strAll = strAll.substring(0, strAll.indexOf(strSrc)) + strDest + strAll.substring(strAll.indexOf(strSrc) + strSrc.length(), strAll.length());
		}
		return strAll;
	}

	private String toDate() {
		SimpleDateFormat dateFormat=new SimpleDateFormat("'#SERVERDATE#'yyyy/MM/dd/HH/mm/ss");
		java.util.Date currentDate = new java.util.Date();
		return dateFormat.format(currentDate);
	}

	final String toKor (String en){
		if(en==null){
			return "";
		}
		try{
			return new String(en.getBytes("8859_1"), "KSC5601");
		}catch(Exception e){return en;}
	}
}
%>
