<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="loti.courseMgr.mapper.ResultHtmlMapper">
	<resultMap type="DataMap" id="resultDocRow">
		<result property="CONTENT"  column="CONTENT" javaType="String" jdbcType="CLOB" typeHandler="org.apache.ibatis.type.ClobTypeHandler"></result>
	</resultMap>
	
	<select id="selectResultDocRow" parameterType="int" resultMap="resultDocRow">
	<![CDATA[
		-- 수료증 HTML 상세보기
		SELECT 
			NO, TITLE, CONTENT, 
			USE_YN, LUSERNO, LDATE
		FROM TB_RESULT_DOC
		WHERE 1=1
			AND NO = #{no}
	]]>
	</select>
	
	<update id="updateResultDoc" parameterType="Map">
	<![CDATA[
		-- 수료증 HTML 수정
		UPDATE TB_RESULT_DOC SET
			TITLE   = #{title},
			CONTENT = #{content},
			USE_YN  = #{useYn},
			LUSERNO = #{luserno},
			LDATE   = SYSDATE
		WHERE  NO      = #{no}
	]]>
	</update>
	
	<insert id="insertResultDoc" parameterType="Map">
	<![CDATA[
		-- 수료증 HTML 등록
		INSERT INTO TB_RESULT_DOC 
			(NO, TITLE, CONTENT, 
			USE_YN, LUSERNO, LDATE) 
		VALUES 
			( #{no}, #{title}, #{content},
			  #{useYn}, #{luserno}, SYSDATE)
	]]>
	</insert>
</mapper>